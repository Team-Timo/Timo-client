import { FocusTimerContainer } from "@/app/[locale]/(main)/focus/_containers/FocusTimerContainer";

export default function FocusPage() {
  return (
    <section className="border-timo-gray-500 flex h-full items-center justify-end border-l bg-white">
      <FocusTimerContainer />
    </section>
  );
}
